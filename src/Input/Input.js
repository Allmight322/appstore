import React from 'react';
import classes from "./Input.module.css"
const Input = () => {
    return (
        <div>
            <input className={classes.content}/>

        </div>
    );
};

export default Input;